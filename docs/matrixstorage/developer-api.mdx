---
title: MatrixStorage 开发者 API
description: MatrixStorage 当前开发者入口、代码树与 Javadoc 生成方式。
---

# MatrixStorage 开发者 API

MatrixStorage 当前稳定公开层：

- `com.y54895.matrixstorage.api`
- `com.y54895.matrixstorage.api.model`

内部代码仍主要按这些分层组织：

- `MatrixStorage`
- `MatrixStoragePlatform`
- `bridge/`
- `integration/`
- `model/`
- `service/`
- `storage/`
- `ui/`

## 代码树

```text
src/main/kotlin/com/y54895/matrixstorage/
├─ MatrixStorage.kt
├─ MatrixStoragePlatform.kt
├─ api/
├─ bridge/
├─ command/
├─ integration/
├─ listener/
├─ metrics/
├─ model/
├─ service/
├─ storage/
└─ ui/
```

## 生成 Javadoc

```bash
cd /www/codex-work/MatrixStorage
bash ./gradlew generateJavadoc
```

输出目录：

```text
build/dokka/javadoc/
build/libs/MatrixStorage-<version>-javadoc.jar
```
